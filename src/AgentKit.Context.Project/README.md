# AgentKit.Context.Project

Workspace project instruction discovery for AgentKit context assembly.

Register with `AddProjectInstructionContributor` after `AddAgentContext` and an
`IFileSystem` implementation. See [ServiceExtensions.cs](ServiceExtensions.cs).
