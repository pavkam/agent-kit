interface:
  display_name: "AgentKit Diagnostics"
  short_description: "Trace and debug agent executions"
  default_prompt:
    "Use $agentkit-diagnostics to investigate an AgentKit runtime or integration
    failure."
